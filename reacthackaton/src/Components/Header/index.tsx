import  "./estilo.css"

interface PropsHeader {
    // abrirModal: () => void;
}


export const Header = (props: PropsHeader) => {
    return(
     
        <header>
            <h1>Transporte publico</h1>
        </header>
    
     
    )
}
